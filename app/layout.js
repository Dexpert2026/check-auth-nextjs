import "./globals.css";

export const metadata = {
  title: "Check Alpha ::: Product Authentication :::",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
